import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // swcMinify: false, // Ensure that SWC minification is disabled
  // turbopack: false, // Explicitly disable Turbopack
  
  /* config options here */
};

export default nextConfig;
